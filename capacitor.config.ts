import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'expresstrans.com',
  appName: 'Express Trans',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
